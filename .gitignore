# 依赖与构建产物
node_modules
dist
dist-ssr
*.local

# 编辑器与系统
.DS_Store
*.log
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea

# TypeScript 增量构建缓存
*.tsbuildinfo

# wrangler 的本地状态：本地 D1 的 sqlite、缓存、日志。
# 里面是「我这台机器上调试用的数据」，不该进仓库 —— 而且体积会长。
.wrangler
